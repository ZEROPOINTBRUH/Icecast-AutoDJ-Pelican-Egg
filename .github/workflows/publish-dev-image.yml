name: Publish Dev Image

# DISABLED: Manual builds via scripts/build-and-push.ps1 recommended
# This workflow requires additional GHCR permissions setup
# For now, manual Docker push is more reliable

on:
  workflow_dispatch  # Manual trigger only

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Disabled
        run: echo "This workflow is disabled. Use local build script instead."
